import pygame
from random import randint

class Blocks(pygame.sprite.Sprite):
    def __init__(self):
        # Creating image for the block
        # self.image = pygame.image.load().convert_alpha()
        # Scaling the 
        # self.image = pygame.transform.scale(width,height)
        # self.rect = self.image.get_rect(center = (x_coord,y_coord))
        pass

    '''def __init__(self, width, height, x_coord, y_coord):
        # Creating image for the block
        self.image = pygame.image.load().convert_alpha()
        # Scaling the 
        # self.image = pygame.transform.scale(width,height)
        # self.rect = self.image.get_rect(center = (x_coord,y_coord))
        print('im gey')'''